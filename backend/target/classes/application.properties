# Puerto dinámico de Railway
server.port=${PORT:8080}
server.address=0.0.0.0

# Configuración de errores (oculta detalles por seguridad)
server.error.include-message=never
server.error.include-stacktrace=never
server.error.whitelabel.enabled=false

# Configuración de sesión
server.servlet.session.timeout=${SESSION_DURATION_MS:43200000}

# Logs para debugging (puedes quitarlo después)
logging.level.org.springframework.security=INFO
logging.level.org.springframework.web.cors=DEBUG

## Allowed CORS origins for the frontend in development
##app.cors.allowed-origins=http://localhost:5173logging.level.org.springframework.security=DEBUG
